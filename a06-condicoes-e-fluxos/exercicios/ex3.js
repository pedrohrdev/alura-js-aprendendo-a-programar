/*
3. Verificação de número positivo ou negativo
Crie uma variável com um número qualquer. Mostre
se ele é positivo, negativo ou igual a zero.
*/;

const numero = -1;

if(numero > 0) {
    console.log(`${numero} é positivo`)
} else if (numero === 0) {
    console.log(`${numero} é igual á 0`)
} else {
    console.log(`${numero} é negativo`)
}