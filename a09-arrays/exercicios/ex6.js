/*
6. Mensagens personalizadas com forEach

Crie um array com nomes de usuários. Use forEach
para imprimir "Olá, [nome]!" para cada um.
*/

const nomes = ['Pedro', 'Neymar', 'Messi'];

nomes.forEach((nome) => {
    console.log(`Ola ${nome}!`);
});
