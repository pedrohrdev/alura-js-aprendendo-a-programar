/*
10. Cálculo de compra
Crie um objeto produto com preco e quantidade, e calcule o valor total da compra(preco * quantidade).
*/

const produto = {
    preco: 100,
    quantidade: 3 
};

console.log(`Valor total da compra: ${produto.quantidade} tenis, valor individual: ${produto.valor}, total da compra: ${produto.preco * produto.quantidade}`)